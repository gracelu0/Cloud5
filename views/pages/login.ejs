<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" charset="utf-8">
    <title>Hidden Traps</title>
    <link rel="stylesheet" href="/stylesheets/forms.css">
    <link href="https://fonts.googleapis.com/css?family=Press+Start+2P&display=swap" rel="stylesheet">
  </head>
  <body>

    <h1 align="center">HIDDEN TRAPS</h1>

    <% if ( typeof signupMessage != 'undefined' && signupMessage.length > 0) { %>
    <p class="successMessage"><%= signupMessage %></p>
    <% } %>

    <!--modal content-->
    <form class="" action="/login" method="post">
      <div class="container">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" placeholder="Enter username" maxlength="30" required>

        <label for="pwd">Password</label>
        <input type="password" id="pwd" name="pwd" placeholder="Enter password" maxlength="30" required>

        <% if ( typeof loginMessage != 'undefined' && loginMessage.length > 0) { %>
        <p class="errorMessage"><%= loginMessage %></p>
        <% } %>

        <button type="submit" name="signInBtn">Login</button>
    </form>
    <form class="" action="/signUp" method="post">
      <button type="submit" name="signUpBtn">Sign up</button>
    </form>

  </body>

</html>
